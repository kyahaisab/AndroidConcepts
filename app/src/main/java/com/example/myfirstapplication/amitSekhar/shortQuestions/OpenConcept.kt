package com.example.myfirstapplication.amitSekhar.shortQuestions

// We can mark a class, fun, variable as open
// In kotlin classes are final by default so to inherit them we need to use open keyword
// By default functions are final, to override a fun of superclass we need to use open keyword in fun
// variables are final by default in kotlin, to override them in inherited class make it open in parent class

// In java opposite convention is followed